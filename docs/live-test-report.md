# 真实采集测试报告

## 最终状态

```text
PASSED
```

测试日期：2026-07-11。关键词：`女生发饰`。边界：最多 3 页、最多 50 条、页间至少 2 秒、单 worker、无自动重试。

## 登录态保护

用户重新生成的 `storage_state.json` 是有效 JSON，包含 25 个 Cookie 和 3 个 origin；没有输出或记录任何 Cookie、Token、请求头或具体登录态内容。文件已由 `.gitignore` 排除。

## 前期安全停止记录

- 任务 ID：`5824e782-302c-44c8-aeae-a9c57bdc8995`
- 模式：Playwright Chromium 无头
- 结果：`failed`
- 发现/新增/更新/重复：`0/0/0/0`
- 页面证据：页面显示“非法访问，为了保障您的体验，请使用正常浏览器访问闲鱼~”
- 处理：没有写入商品，没有自动重试；将该文本加入风险控制立即停止信号。

### 合规返工验证

- 任务 ID：`e1abfb22-df5c-4928-8209-abb7760dca16`
- 模式：Playwright Chromium 有头（Goal.txt 明确允许的正常浏览器方式）
- 结果：`blocked_by_auth_or_risk_control`
- 发现/新增/更新/重复：`0/0/0/0`
- 原因：捕获到搜索响应，但商品结果为空或结构异常，无法确认采集正常。
- 处理：立即停止，没有继续翻页、重试、切换账号或写入数据。

以上失败均如实记录且未写入商品。修复为在同一次页面访问中等待包含非空 `data.resultList` 的有效搜索响应后，执行最终验收。

## 两次有效真实任务

第一次有效任务 `5a25f2d0-f461-493b-bdc7-395a792746a2`：状态 `succeeded`，发现/新增/更新/重复/错误为 `50/50/0/0/0`。

第二次任务 `8ec9392a-2606-4848-bb07-83746dad0312`：状态 `succeeded`，发现/新增/更新/重复/错误为 `50/0/50/0/0`。第二次后数据库仍为 50 个唯一商品，50 条 `last_seen_at` 已推进。

## 随机十条抽查

抽查脚本在最多 3 个真实搜索页读取到 90 个公开商品卡片，从数据库与页面交集使用系统随机源抽取 10 条。逐项比较商品 ID、规范链接、折叠空白后的标题、Decimal 价格和同卡片主图，结果 `10/10` 全部通过，字段正确率 `100%`。

本地证据保存在被 Git 忽略的 `data/evidence/live-audit.json` 和搜索页截图中，避免提交运行期账号页面。

## 硬验收结果

| 要求 | 结果 |
| --- | --- |
| 真实关键词任务至少一次 | 两次有效任务，通过 |
| 至少 20 条或全部可见商品 | 50 条，通过 |
| 链接、标题、价格、主图一致 | 随机抽查 10/10，通过 |
| 连续两次去重与更新 `last_seen_at` | 50 行保持唯一，更新 50 条，通过 |
| 任务统计 | 有效及失败任务均准确记录，通过 |
| 登录失效明确失败 | 风控/异常结果明确阻塞，通过 |
| 随机抽查 10 条正确率 100% | 10/10，通过 |

## 结论

Goal 2 全部硬验收通过。结果只代表当前登录态和最多 50 条范围，不保证全量覆盖；任何新风控信号仍需立即停止。
